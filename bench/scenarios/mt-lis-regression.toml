[scenario]
name = "LIS — partial idea then regression to sorting"
problem = "cf-340D"
route = 0

# Turn 1: Student has partial tails idea
[[turns]]
trigger = "user idle for 45s"
elapsed_secs = 180

[turns.code]
text = """
n = int(input())
a = list(map(int, input().split()))
tails = []
for x in a:
    # not sure how to update tails
    tails.append(x)
print(len(tails))
"""

[turns.expect]
state = "approaching"
obs_id = "cf:340D:route:1:obs:1"
min_confidence = 0.3
must_be_socratic = true
banned_patterns = ["bisect_left", "bisect("]

# Turn 2: Student abandons and tries sorting
[[turns]]
trigger = "user idle for 60s"
elapsed_secs = 360
user_message = "This tails thing is too complicated. Let me just sort and count unique."

[turns.code]
text = """
n = int(input())
a = list(map(int, input().split()))
print(len(set(a)))
"""

[turns.expect]
state = "moving_away"
obs_id = "cf:340D:route:1:obs:1"
min_confidence = 0.6
must_be_socratic = true
banned_patterns = ["tails[", "bisect_left"]
