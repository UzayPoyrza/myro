[scenario]
name = "Watermelon — checks only even, misses w=2 edge case"
problem = "cf-4A"
route = 0
trigger = "test failure on test 3"
elapsed_secs = 60
observations_found = 1

[scenario.obs_states]
0 = "found"

[scenario.code]
text = """
w = int(input())
if w % 2 == 0:
    print("YES")
else:
    print("NO")
"""

[expect]
state = "approaching"
min_confidence = 0.5
must_be_socratic = true
banned_patterns = ["w > 2", "w == 2", "w != 2"]
