[scenario]
name = "Theatre Square — empty code, stalled 60s"
problem = "cf-1A"
route = 0
trigger = "user idle for 60s"
elapsed_secs = 60
observations_found = 0

[scenario.code]
text = """
n, m, a = map(int, input().split())
"""

[expect]
state = "uncertain"
min_confidence = 0.0
max_confidence = 0.5
must_be_socratic = true
banned_patterns = ["(n + a - 1) // a", "ceil(n/a) * ceil(m/a)", "math.ceil"]
